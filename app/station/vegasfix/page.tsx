import AutoClaimStation from "@/components/AutoClaimStation";

export default function VegasFixPage() {
  return (
    <AutoClaimStation
      station="vegasfix"
      title="Vegas Gold Call"
      fact="Gold is priced twice daily via the LBMA Gold Price, the world's benchmark gold price."
    />
  );
}
